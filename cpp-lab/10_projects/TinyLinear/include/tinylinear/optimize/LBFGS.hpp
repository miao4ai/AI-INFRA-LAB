#pragma once

// TinyLinear — Limited-memory BFGS quasi-Newton optimizer
//
// Part of the optimize module. Header-only declaration stub; the
// implementation lives in src/optimize/ as the corresponding phase is built.

namespace tinylinear::optimize {

// TODO(LBFGS): declare the LBFGS interface.

}  // namespace tinylinear::optimize
