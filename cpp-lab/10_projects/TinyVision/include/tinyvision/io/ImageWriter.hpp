#pragma once

// TinyVision — Abstract image writer interface
//
// Part of the io module. Header-only declaration stub; the
// implementation lives in src/io/ as the corresponding phase is built.

namespace tinyvision::io {

// TODO(ImageWriter): declare the ImageWriter interface.

}  // namespace tinyvision::io
