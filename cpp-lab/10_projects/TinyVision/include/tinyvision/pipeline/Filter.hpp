#pragma once

// TinyVision — Filter interface — a single processing stage
//
// Part of the pipeline module. Header-only declaration stub; the
// implementation lives in src/pipeline/ as the corresponding phase is built.

namespace tinyvision::pipeline {

// TODO(Filter): declare the Filter interface.

}  // namespace tinyvision::pipeline
