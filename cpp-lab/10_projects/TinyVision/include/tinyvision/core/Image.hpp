#pragma once

// TinyVision — Owning 2D image (width, height, channels, pixel storage)
//
// Part of the core module. Header-only declaration stub; the
// implementation lives in src/core/ as the corresponding phase is built.

namespace tinyvision::core {

// TODO(Image): declare the Image interface.

}  // namespace tinyvision::core
