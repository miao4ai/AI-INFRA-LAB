#pragma once

// TinyVision — Histogram calculation and equalization
//
// Part of the processing module. Header-only declaration stub; the
// implementation lives in src/processing/ as the corresponding phase is built.

namespace tinyvision::processing {

// TODO(Histogram): declare the Histogram interface.

}  // namespace tinyvision::processing
