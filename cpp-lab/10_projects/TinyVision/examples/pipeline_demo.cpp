// TinyVision example — Pipeline demo — chain filters into a pipeline
//
// TODO: implement once the relevant TinyVision modules are ready.

int main() {
    // Pipeline demo — chain filters into a pipeline
    return 0;
}
