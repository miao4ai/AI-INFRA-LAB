#include "product.h"

#include <iostream>
#include <vector>

// Helper to print a matrix (you can flesh this out).
static void print(const std::vector<std::vector<double>>& M) {
    // TODO(萌喵酱): print each row of M
    (void)M;
}

int main() {
    // TODO(萌喵酱):
    //   1) build a sample A (e.g. 2x3) and B (3x2)
    //   2) auto C = product(A, B);
    //   3) print(C);  and check it matches the expected result
    return 0;
}
